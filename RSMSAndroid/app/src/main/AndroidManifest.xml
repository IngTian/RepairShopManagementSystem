<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.mcgill.ecse321.rsms_android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RSMSAndroid">
        <activity android:name=".SignUpActivity"></activity>
        <activity android:name=".SelectServiceAndCar" />
        <activity android:name=".ShowShifts" />
        <activity android:name=".AppointmentPaymentActivity" />
        <activity android:name=".AppointmentViewAndPayActivity" />
        <activity android:name=".MakeAppointmentSelectDate" />
        <activity android:name=".MakeAppointmentActivity" />
        <activity
            android:name=".EditInfoActivity"
            android:label="@string/title_activity_edit_info"
            android:theme="@style/Theme.RSMSAndroid.NoActionBar" />
        <activity
            android:name=".CarMActivity"
            android:label="@string/title_activity_car_m"
            android:theme="@style/Theme.RSMSAndroid.NoActionBar" />
        <activity
            android:name=".homepageActivity"
            android:label="@string/title_activity_homepage"
            android:theme="@style/Theme.RSMSAndroid.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.RSMSAndroid.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>